This demonstrates how to use the `gi-rsvg` library to display an SVG file in
a Cairo canvas.

Until the `gi-cairo-render` library dependencies catch up you may need to run this with

`cabal run rsvg-example --allow-newer`
